{
  "name": "homebridge-reolink-motion",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf ./dist",
    "prebuild": "npm run clean"
  },
  "dependencies": {
    "mqtt": "^5.0.0",
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "@types/mqtt": "^4.2.6",
    "@types/node-fetch": "^2.5.12",
    "rimraf": "^3.0.2",
    "typescript": "^4.9.0"
  },
  "engines": {
    "node": ">=14.0.0",
    "homebridge": ">=1.0.0"
  }
}
